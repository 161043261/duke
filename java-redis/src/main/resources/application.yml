spring:
  application:
    name: java-redis

  data:
    redis:
      host: 127.0.0.1
      port: 6379
      # password:
      lettuce:
        pool:
          max-active: 8 # 最大连接数
          max-idle: 8 # 最大空闲连接数
          min-idle: 0 # 最小空闲连接数
          max-wait: 100 # 最长连接等待时间
